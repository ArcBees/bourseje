.batman {
    margin: 0 auto 25px;
    width: 105px;
    height: 120px;
}

.eye {
    stroke-width: 0;

    @mixin animation_name(eye);
    @mixin animation_duration(.75s);
    @mixin animation_delay(.5s);
}

@-webkit-keyframes eye {
    to {
        stroke-width: 1;
    }
}
